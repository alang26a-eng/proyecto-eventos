PORT=8080
NODE_ENV=development
# MongoDB local. Para Atlas reemplazar SOLO en .env con la URI privada.
MONGO_URL=mongodb://127.0.0.1:27017/eventhub
# Reservado para próximas entregas; el registro todavía no usa JWT.
JWT_SECRET=reemplazar_por_un_secreto_local

